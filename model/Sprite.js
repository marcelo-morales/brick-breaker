import Block from "./Block.j"

// does everything that Block does + moves -> so do inheritance !
class Sprite {
    constructor(x, y, width, height, color, dx, dy) {
                
            uper(); 
    }
    move()
}

export default Sprite;